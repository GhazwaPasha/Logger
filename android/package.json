{
  "name": "@work-ledger/android-twa",
  "private": true,
  "description": "LogBase Android app: Trusted Web Activity wrapper around the production PWA (project generated by Bubblewrap).",
  "scripts": {
    "update": "bubblewrap update --skipVersionUpgrade",
    "build": "node scripts/build.mjs",
    "emulator": "node scripts/emulator.mjs"
  },
  "devDependencies": {
    "@bubblewrap/cli": "^1.24.1"
  }
}
